# CRITICAL: playwright version must match the Docker image tag exactly.
# Docker image: mcr.microsoft.com/playwright/python:v1.60.0-noble
playwright==1.60.0
playwright-stealth==2.0.3

# API layer
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4

# Tier 3: TLS fingerprint spoofing (lightweight fallback, no JS)
curl-cffi==0.15.0
cloudscraper==1.2.71

# Testing
pytest==9.0.3
pytest-asyncio==1.4.0
httpx==0.28.1
