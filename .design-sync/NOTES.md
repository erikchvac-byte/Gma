# design-sync notes — Gma's Helper Design System

- **Provenance / direction:** the remote project (45dd119c…) is the ORIGIN of this design system; Erik exported it ("My UI idea/", since deleted; archived copy in the UX workspace imports/). Sync direction is local ruled edits → remote. Canonical local copy: `design-system/`.
- **Rulings source:** `_bmad-output/planning-artifacts/ux-designs/ux-Happy-2026-06-11/` (DESIGN.md + EXPERIENCE.md final, .decision-log.md). Spines win over anything in design-system/.
- **2026-06-12 update set:** `--border-field` token (gray-500) on TextField/Select; stale badge gray-600; slider 44px hit target (padding/negative-margin/content-box trick in `.gma-range__input`); amber AA-pin comment; data.js staleCount derived from strict predicate (wild-seed marked stale); settings.jsx → stacked Selects + auto-apply (no Save button) per FR-8; feed.jsx disclaimer footer + `<time>`; readme.md corrections (28 ADRs, borders, no-Save, scraped-copy rule, disclaimer); CLAUDE.md + SKILL.md authored fresh from the spines and real `.d.ts` (the originals had fictional prop tables and a wrong radius claim — never restored from the export; auto-mode classifier also blocks restoring them verbatim).
- **`_ds_bundle.js` deliberately NOT respliced:** it embeds Babel-compiled copies of the ui_kits demo files (incl. the old Save-button sheet), but `ui_kits/app/index.html` loads the source `.jsx` AFTER the bundle, overriding those window assignments — the demo renders the updated behavior. The `GmaSHelperDesignSystem_45dd11` namespace exposes only the 10 primitives, whose sources are unchanged (all 2026-06-12 component fixes are CSS-side). Hand-transpiling a splice risks breaking the runtime every design renders against. If the primitives' JSX ever changes, the bundle must be regenerated properly.
- **`CLAUDE.md` is a reserved path** — the sync API blocks writing it (design-agent instruction channel). The remote project's CLAUDE.md therefore still carries the export-era errors (radius-md claim, fictional prop tables); the corrected copy lives locally at `design-system/CLAUDE.md`, and the corrections that matter to the design agent are in the uploaded `readme.md`. Fixing the remote CLAUDE.md requires editing in the claude.ai/design app itself.
- **No `_ds_sync.json` uploaded:** off-script shape (no converter run, no screenshot verification harness) — omitting the anchor is the honest choice; a future converter-based sync re-verifies everything.
- **Verification level (2026-06-12):** textual/name validation only (every class/token/prop in docs greps against tokens/components.css/.d.ts; demo logic reviewed). No browser screenshot harness available on this machine — visual check of the cards is on Erik in the Design System pane after upload.
