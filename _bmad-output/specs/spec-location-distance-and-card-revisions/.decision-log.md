# Decision Log — spec-location-distance-and-card-revisions

## 2026-06-26 — Create (express mode, one clarifying question)

**Input:** Erik's in-chat change request ("NEED TO CHANGE SOME OF WHAT WE HAVE DONE") naming four changes: (1) store address on the card upper-left at product-name size; (2) gate distance/$ on user location, with a better way to get miles from the user's location/input; (3) remove the hardcoded 28-MPG national default; (4) make the hidden vehicle dropdown obvious.

**Operation:** Create. Grounded in the live code (App/DealFeed/DealCard/VehicleSelector/gasCost/useVehicleMpg, types, data.json) and the ADRs. Found the request is the consumer-facing half of the already-planned ADR-044 "User-Relative Positioning" (deferred-work #2/#3) plus three card/MPG corrections. Lifted the established Geolocation+ZIP+haversine×1.3 mechanism into `mechanism.md` rather than re-deriving it.

**Two questions asked (both changed spec content):**
- Location input → **GPS tap + ZIP box** (two-door, honest empty state). Telephone area code rejected as too coarse.
- No-vehicle behavior → **no car, no gas $** (distance still shows from location). This causally links CAP-4 and CAP-5.

**Decisions of note:**
- 5 capabilities, one per ask plus the location-acquisition mechanism split from the gating rule (CAP-2 gate / CAP-3 acquire).
- Retires ADR-003/013 (national MPG) and ADR-008/011 (fixed origin) — stated as a constraint (remove, not bypass).
- Scope guard: ADR-044 #4 (centroid cold-start) and #5 (slider reframe) held OUT as non-goals — not requested, per Erik's ask-before-expanding rule.
- Address found NOT to be a runtime field today (lives only in the geocode dev-map) → CAP-1 carries a real schema-promotion + sourcing dependency.

**Self-validate — Pass 1 (Coherence):** All 5 caps have intent + success; intents are WHAT (HOW pushed to `mechanism.md`); constraints each rule something out (Honest Math, both-inputs-for-gas, retire-not-bypass, no paid services, one formula, real-address-only); non-goals explicit (5); success signal is demonstrable end-to-end. PASS.

**Self-validate — Pass 2 (Preservation):** All four source asks landed (CAP-1 address; CAP-2/3 location-gated real distance; CAP-4 MPG removal; CAP-5 obvious vehicle input). The user's exact example string and "upper-left / product-name size" preserved in Assumptions + mechanism. Three load-bearing gaps the input did not resolve → Open Questions (card layout, 4-originals address sourcing, fueleconomy.gov now load-bearing). No load-bearing claim dropped. Wrapper-only content: none.

**Verdict:** Coherent and preservation-complete. Ready for downstream (UX resolve on layout/affordance, then build). Three open questions should be answered before implementation; none block the spec.

## 2026-06-26 — Update (open-question resolutions)

- **Address sourcing — RESOLVED.** Reading `server/scripts/geocodeStores.ts` showed all 21 stores (incl. the 4 originals) already carry cited street addresses in `STORE_ADDRESSES`. So no new sourcing is needed; the build promotes them into the runtime schema (`Dispensary.address`) + `data.json`. Erik to spot-verify the 4 originals. Removed from Open Questions.
- **fueleconomy.gov hardening — RESOLVED (in scope).** Erik folded the known reliability gaps (timeout, spinner, retry, empty-result message) into this work. Promoted from an Open Question to a Constraint.
- **Card layout — RESOLVED via mockup.** Erik: address top-**right** at deal-title size (a flip from the original "upper-left" ask); the **distance pill tucks beneath the address**; the gas-trip line is pushed down below. With no location, address is the only top-right element. CAP-1, Success signal, mechanism, and Open Questions updated; no live open questions remain. Spec is build-ready.
