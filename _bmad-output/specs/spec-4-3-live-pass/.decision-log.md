# Decision Log — spec-4-3-live-pass

## 2026-06-14 — Execution COMPLETE (bmad-dev-story)

Ran the live pass against the live Python service (Windows gotcha: `python` resolves to the Store shim; real interpreter is `…\Programs\Python\Python313\python.exe` — all deps already installed). All 5 capabilities verified live. Key event: the synthesized fixture/transform were wrong on every field (CAP-3 material delta) → surfaced and stopped per the frozen boundary; Erik approved the rework + chose parse-days-if-detected-else-everyday. Reworked `_dutchie.ts` (real `getSpecialMenuCards.menuCards` shape; text-parsed discount/days; `happy_hour` deferred — no live `recurringSchedule`), fixed a wait-pattern bug (wait on `GetSpecialMenuCards`, not first GraphQL — was dropping kush21), replaced the fixture with a real capture, reworked tests, wired both ids. Result: 3/3 Dutchie stores flow live deals; graceful degradation preserves deals on service-down. 86 server tests pass, tsc clean. ADR-030 added; deferred-work two items cleared (one new: timed-special support); sprint-status `4-3-live-pass: done`. Findings: `live-findings-2026-06-13.md`. Open question (unresolvable id → BLOCK) never triggered — both ids resolved.

## 2026-06-13 — Create (interactive, express distill)

**Source inputs:** `4-3-dutchie-iframe-dispensary-support.md` (Design Notes + frozen Intent/Boundaries), `deferred-work.md` (the two live-pass items + evidence-fixture item), `epic-4-context.md` (contract + service decisions), `C:\Users\erikc\Dev\Scraper\HANDOFF.md` (service API, run commands, /discover, embed-URL knowledge).

**Erik's four shaping decisions:**
1. Done bar = **all three stores live**; an unresolvable embed ID BLOCKS + escalates (no silent re-defer). → Success signal + Constraint + CAP-2/CAP-4.
2. Shape delta = **surface and stop** (honor frozen 4.3 boundary). → CAP-3 + Constraint 1; transform never silently reworked.
3. Evidence-fixture relocation = **out of scope**. → Non-goal.
4. Executor = **dev agent**. → Machine-contract capabilities with testable success; runbook companion supplies the concrete service-bring-up/`/discover` procedure the agent still needs.

## 2026-06-13 — Post-create corrections (codebase verification)

Verified against `server/` before handing off. Two fixes landed in SPEC + runbook:
1. **Entrypoint:** there is no `npm run scrape`. `runScrapers` runs on server boot (`server/index.ts:32`) + hourly; the live run is triggered by `cd server && npm run dev` (boot scrape). Runbook step 3 + SPEC assumption corrected. A one-off `scrape` script is out of scope unless Erik approves.
2. **Zero-specials assumption was wrong.** `runScrapers.ts:42` maps an empty/normalized-away return to `stale:true` + `error: scraper returned no deals`, and `normalizeDeals` drops degenerate deals before the count. So a genuinely-zero-specials store is indistinguishable from a scrape failure in `data.json` (only `logs.json` distinguishes) → escalate, not silently "done." SPEC assumption + runbook note corrected.

Also: assumptions + open-question appended to the story record (`4-3-…md`, new non-frozen "Live Pass — Spec Reference" section); `deferred-work.md` annotated to point at the spec (items NOT struck — pass not yet run).

**Verdict (Pass 1 Coherence / Pass 2 Preservation):** 5 capabilities, each with intent + success; constraints all rule something out (frozen contract, no-service-edits, no-orchestrator-edits, embed-only navigation, ID-block escalation, offline tests); 5 explicit non-goals; success signal is demonstrable (real deals in data.json + GET /api/data + logs ok). Preservation pass walked every load-bearing source claim — embed-ID resolution, fixture reconciliation (path/discount-unit/time-window), end-to-end flow, graceful degradation, service-boundary rules, HANDOFF run/discovery procedure — each lands in a capability, constraint, non-goal, or the runbook companion. No load-bearing claim dropped. One companion produced (`live-pass-runbook.md`, spec-authored). Two `assumptions[]` (service runs locally on :8000; the-joint id still valid; zero-specials is a valid real result) and one `open_question` (fallback when an embed ID is undiscoverable — default BLOCK/escalate) carried forward. Wrapper-only content: none.
