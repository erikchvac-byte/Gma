# Decision Log — spec-dutchie-product-pricing

## 2026-06-24 — Create

**Operation:** Create from a rich in-chat brief (Erik) + the live-verified investigation `menu-pricing-source-inventory.md`. Express extraction; no elicitation needed (input was complete). Slug `dutchie-product-pricing`.

**Companion decision:** The verified `FilteredProducts` field shape, alignment rules, pre-roll filter, and hazard list already live in the investigation. Adopted it as a companion (relative path, not edited) rather than duplicating the field table into a spec-authored companion. No diagrams → no `architecture-diagrams.md`. No spec-authored companions.

**Pass 1 — Coherence (Spec Law 1–6, 8):** All four capabilities carry intent + success. CAP-1's intent names "a new module separate from the specials scrape" — borderline HOW, but the decoupling IS the load-bearing intent of this work, so retained. Constraints all bend design (forbidden-file list, new-module mandate, price-source rule, test gate, legal blocker). Five non-goals. Success signal is testable (real price number + byte-identical deals contract + 177 tests green). IDs CAP-1..4, fresh. Prose lean. Pass.

**Pass 2 — Preservation:** Every load-bearing source claim landed — op/path/shape (constraint + companion), Options↔Prices↔recSpecialPrices alignment (CAP-2 + constraint), children-base/positional-special rule (constraint), empty-products race + retry (CAP-3), pagination (CAP-3), 21-store volume (Why + blocker), legal verify-with-counsel gate (blocker constraint), the full decoupling guarantee set incl. 177-test regression net (constraints), planning-only (non-goal + blocker). No load-bearing drops. No wrapper-only content to log.

**Verdict:** Coherent and preservation-complete. Three open questions logged (refresh cadence/storage; capture scope — pre-rolls vs whole menu; whether counsel constrains retained fields like live `quantityAvailable`). Two assumptions logged (rec-pricing-first; reuse existing ingest/schedule). These are genuine human decisions, not gaps the input could resolve.

## 2026-06-24 — Update (walk-through of open questions with Erik)

**Answers:** Refresh = lower frequency. Scope = a few key categories. Data posture = REFRAME: Erik is building a backend price-comparison engine + his own longitudinal dataset, not a price display. Core need = normalize to true unit economics (price per gram, price per item/joint) accounting for PACK count (3-pack vs 5-pack) and weight; pack count often lives in free-text `Name` ("2pk"); track prices over time.

**Spec changes:** Why rewritten to the comparison-engine/dataset framing. Added CAP-5 (unit-economics normalization incl. name-embedded pack parsing, safe-degrade on unparseable) and CAP-6 (timestamped price-observation history, change-tracking, export). Added constraints: parse pack/weight into structured quantity + reconcile the unresolved raw `weight` unit (sample `weight:1000` for a "2g"/2-pack → likely per-unit mg) before trusting per-gram math; storage retains a time series, not just latest. Success signal rewritten to cross-store best-value ranking + history. Assumptions/open-questions refreshed (category set, per-category comparison basis, time-series granularity/retention, counsel field constraints).

**Note for source companion:** The investigation's two flagged uncertainties — `weight:1000` unit and pack-count-in-`Name` — are now LOAD-BEARING for normalization (CAP-5). Offered to annotate the investigation accordingly.

**Verdict:** Still coherent + preservation-complete. CAP-5/6 each carry intent+success; new constraints bend design (parsing mandate, time-series storage). Open questions are genuine human/counsel decisions.

## 2026-06-24 — Update 2 (refined walk-through)

**Answers:** Categories = Pre-Rolls + Flower + Vaporizers (Edibles/Concentrate excluded at launch). Comparison = per-category canonical unit (Flower/Vape $/gram; Pre-Rolls $/gram + $/joint). History = append every scrape (max granularity). Edibles being out removes the mg-THC basis complication — all three launch categories are weight/pack-based.

**Spec changes:** Assumptions firmed (exact category set, per-category unit basis, every-scrape history). Two prior open questions resolved (category set; comparison basis). Remaining open questions narrowed to: time-series retention horizon + export format; counsel field constraints.

**Verdict:** Coherent + preservation-complete. Two open questions remain — one operational (retention/export), one gated on counsel. No further capability changes.

## 2026-06-24 — Update 3 (counsel gate downgraded for R&D)

**Decision (Erik):** Remove the verify-with-counsel clearance as a hard implementation BLOCKER; keep it as a non-blocking reminder. Rationale: this is R&D / a private dataset Erik owns — his risk call.

**Spec changes:** Deleted the BLOCKER constraint and converted it to a `> Reminder (non-blocking)` callout after Constraints (revisit before any public surfacing / redistribution / commercial use). Why no longer says "gated on"; the legal dimension is now a carried reminder. Non-goal "implementation is held behind the gate" → "building is the next phase, no longer counsel-gated." Removed the counsel-constraint open question (the review no longer gates the build); retention/export is now the sole open question. Companion investigation updated to match (blockers row + downstream note → non-blocking reminder).

**Verdict:** Coherent + preservation-complete. The reminder still rules a decision (no public/commercial use without review) so it is not pure decoration but is correctly NOT in Constraints as a hard gate. One open question remains (retention/export).
