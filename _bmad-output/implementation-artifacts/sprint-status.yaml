# generated: 2026-06-09
# last_updated: 2026-06-09 (story 1-3 done)
# project: Happy
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-06-09
last_updated: 2026-07-31 (geo-page-sitemap-durability SHIPPED → done — pushed 37b3a7d, Render auto-deploy triggered; bmad-dev-story, ADR-111: decoupled geo /compare URL existence from the request-time 3h freshness overlay so citable pages stop transiently 404ing on ingest jitter; 858/858 server green, builds clean; NEXT code-review + push/deploy). Prior: 2026-07-29 (store-page-crawlable-enrichment story created → ready-for-dev — from the 2026-07-29 Semrush audit; the "21 low word count" remainder after ADR-110 trimmed the long titles. Scopes honesty-safe evergreen prose onto the 18 /store pages [3 region hubs left thin-by-design]; NEEDS Erik go-ahead + a DEV-START enrich-vs-lean ruling). Prior: 2026-07-23 (crawler-visible-homepage-and-agegate DEV COMPLETE → review — SEO Phase 0a-HTML + 0b shipped, ADR-097; Erik released the Phase 0b legal go-ahead on the ADR-066 basis. 599 client + 726 server green, build clean, built-server curl-as-GPTBot verified. Prior: 2026-07-22 oracle-freshness-gate SHIPPED — merged as PR #98 / commit e3cc63f. 721 server green, build clean, ADR-096. AC7 PROVEN on live home DB 2026-07-22: buildMatchReport w/ data-derived anchor 2026-07-22, staleThreshold 2026-07-21 → 2,490 records excluded stale, disparities 1,430 gated vs 1,717 ungated = 287 stale cells suppressed; per-store stale counts sum exactly to 2,490. NOTE: whole-store mt-vernon fall-out NOT reproducible today — happy-time-mt-vernon latest obs = 2026-07-22 (fresh, scraped 3:31 AM), so gate correctly excludes only its 75 individually-stale SKUs, not the store. July-21 mt-vernon staleness was a pre-scrape snapshot, resolved. Gap #2 [WebFetch out of the honesty path] stays parked — NOT to be started until Erik names it.)
project: Happy
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # Epic 1: Project Foundation & Data Layer
  epic-1: done
  1-1-project-scaffold: done
  1-2-seed-dispensary-data: done
  1-3-get-api-data-endpoint-with-active-deal-filtering: done
  epic-1-retrospective: optional

  # Epic 2: Core Deal Experience
  epic-2: done
  2-1-age-gate: done
  2-2-deal-feed-with-active-deals-and-empty-state: done
  2-3-deal-cards: done
  2-4-gas-cost-calculation: done
  2-5-distance-filter: done
  2-6-stale-source-indicator: done
  epic-2-retrospective: optional

  # Epic 3: Gas Cost Accuracy & Personalization
  epic-3: done
  3-1-eia-gas-price-refresh: done
  3-2-vehicle-precision-mode: done
  epic-3-retrospective: optional

  # Epic 4: Live Deal Data via Scraper
  epic-4: done
  4-1-scraper-engine-and-orchestrator: done
  4-2-first-dispensary-html-parsers: done  # re-scoped 2026-06-13 to plain-HTML only (remedy-tulalip); the-joint + jet + kush21 are Dutchie → 4.3
  4-3-dutchie-iframe-dispensary-support: done
  epic-4-retrospective: done

  # Cross-cutting follow-up stories (no parent epic — tracked individually)
  # Promoted from the Epic 4 retrospective (action item 1, Erik 2026-06-13).
  data-hardening: done  # spec-data-hardening.md — client useDeals boundary validation + scraper-ingestion deal normalization
  4-3-live-pass: done  # spec-4-3-live-pass — live E2E: 3/3 Dutchie stores flow real deals; transform reconciled to real shape (ADR-030); all 4 sources data-complete
  5-1-deploy-scraper-service: done  # deploy-READINESS + ADR only (Erik: stay free / defer live 2026-06-15) — env-parametrize SCRAPER_URL + topology ADR-033 + runbook; no Render spend. Review-verified 2026-06-16 (ADR reconcile confirmed).
  compliance-launch-gate: done  # compliance-launch-gate.md — the 3 "verify with counsel" items from regulated-content review: (1) description sanitization+blocklist at normalizeDeals chokepoint [WAC 314-55-155 advertiser-status posture], (2) standing disclaimer footer + flag-gated WA-warning slot, (3) age-gate decline affordance. Two-track: counsel determination (Erik) + defensible engineering posture (dev). LAUNCH-GATING.
  dutchie-product-pagination: done  # dutchie-product-pagination.md — SHIPPED 2026-07-18 (bmad-dev-story `012ace3` + bmad-code-review patches, ADR-089). Numbered-pagination full-menu capture via service-side in-page fetch walk; dead scroll_after_wait removed. Review (3 layers): 6 patches incl. HIGH template-pinning fix (homepage carousels can be productIds-PINNED; selection prefers unconstrained + rewrite clears scoping keys — live proof local-roots 532→2,023), listener detach, 180s paginate timeout, blind-walk tolerance, degrade logging. MEASURED: 17 stores / 5m36s / 23,470 obs (~9× the ~2-2.9k before, +~80s; >54min pre-4AM-derive headroom). Coverage-only (no fact-math/honesty/deals change). Server 705 + scraper-svc 17 green. Expect price-vs-own-median suppression to ease over ~7 days as series accrue. — capture the FULL paginated Dutchie menu instead of the ~100-item embed-homepage cap. Root cause proven: embed menu uses NUMBERED pagination (perPage=100) not infinite scroll, so scroll_after_wait is a no-op; scraper gets only page-1-breadth (~100/store, e.g. happy-time 203 all-time vs flower alone ≈3 pages). Fix = enumerate numbered pages per category (Task-0 spike picks mechanism: Node-side ?page=N multi-call [preferred, no scraper-svc edit] / service-side page-walk / GraphQL replay). Coverage-only: NO fact-math/honesty-gate/deals-path change (AC6/7). Unblocks price-vs-own-median suppression (belowFloor+noObservationToday churn). Ops risk = 5-15x more navigations/store; must fit nightly Scheduled-Task window before 4AM derive. NEEDS Erik go-ahead to BUILD (create-story only planned it).
  commit-back-seed: done  # commit-back-seed.md (Correct-Course 2026-06-22, ADR-047) — make Render's ephemeral-disk reset non-destructive: cron commits scraped data.json back to master as a fresh seed, built via in-CI applyIngest merge (NOT GET /api/data, which filterActiveDeals makes lossy). New commit-back job in scrape-ingest.yml + commitBackSeed.ts + ingestRun emit flag; paths-ignore loop-breaker. Fixes frozen "Last updated Jun 14" / 17-unavailable flip-flop. Out of scope: lowering the unresolved-Dutchie-cName floor.
  crawler-visible-homepage-and-agegate: done  # SHIPPED + LIVE 2026-07-23 (PR #100 squash → master 8b7216d; code-reviewed clean, no blocking findings; Render auto-deployed ~40s). LIVE-VERIFIED: curl -A GPTBot https://gmaslist.com/ shows real stores (Remedy Tulalip, Kush21, The Joint, Jet Cannabis...) + deal text + 21+ line INSIDE #root, __GMA_DATA__ intact, $100 uncorrupted; /about + /compare + browser-UA SPA all unregressed. --- DEV COMPLETE 2026-07-23 (bmad-dev-story, ADR-097). SEO Phase 0a-HTML + 0b shipped: shellRoute.ts server-renders escaped deal/store HTML + static 21+ line INSIDE #root from the same buildApiData() as __GMA_DATA__ (function-replacement $-safe; createRoot wipes on hydration → humans stay gated) → non-JS AI crawlers see deals; AgeGate.tsx + App.tsx always mount children marked React-19 `inert`+aria-hidden behind the unchanged opaque overlays (zero human visual change) → JS crawlers (Googlebot) see the feed, NOT the onboarding prompt (both content gates handled). Additive/honesty-preserving (no /api/data/Deal/data.json change). 599 client + 726 server green; real npm run build clean; built-server curl -A GPTBot verified (real stores/deals + 21+ line inside #root, __GMA_DATA__ intact, $100 uncorrupted). Closed the deferred 0a-crawler-HTML item. NEXT: code-review (recommend a different LLM), then push/deploy (Erik's standing go-ahead) + Lighthouse LCP re-run + Search Console. — SEO Phase 0a-HTML + 0b (the ~90% crawler-visibility unlock). Story created 2026-07-23 (bmad-create-story); RELEASES the Phase 0b legal go-ahead (Erik 2026-07-23, ADR-066 basis). Two coupled changes: (0a-HTML) shellRoute.ts server-renders escaped semantic deal/store HTML INSIDE #root (crawler-only sink createRoot wipes on hydration) from the same buildApiData() as __GMA_DATA__ → non-JS AI crawlers (GPTBot/Perplexity/OAI/Claude-SearchBot) see deals; (0b) AgeGate.tsx stops withholding children — always mounts them behind the unchanged opaque overlay via React 19.2 native `inert`+aria-hidden while gated (zero human visual change) → JS crawlers (Googlebot) see them too. LOAD-BEARING FINDING: a SECOND gate (LocationOnboarding in App.tsx) also withholds DealFeed from the DOM until a click, so 0b alone leaves a fresh JS crawler on the onboarding step — story includes the same inert-overlay fix for it (AC7, confirm scope at dev-start). Escaping/fail-soft/function-replacement rules inherited from the shipped snapshot injection. 2 DEV-START Qs for Erik: (1) LocationOnboarding fix in scope now [recommended yes]; (2) static 21+/WA-warning line in crawler body [counsel call, mirrors /about deferred item]. Closes the two deferred 0a-crawler-HTML items in deferred-work.md; new ADR for impl + 0b go-ahead. NEEDS Erik go-ahead to BUILD (create-story only planned it).

  # Epic 6: Apply Gma's Helper Design System to the client
  # Split ruled with Erik 2026-06-16: foundation → primitives → surface re-skin.
  # Source of truth: planning-artifacts/ux-designs/ux-Happy-2026-06-11/{DESIGN.md,EXPERIENCE.md} + imports/gmas-helper-design-system.
  epic-6: done
  6-1-design-system-foundation: done  # token layer + Tailwind v4 @theme + self-hosted fonts + base.css; no component/surface changes. Reviewed/cleared 2026-06-16 (status-drift fix).
  6-2-primitive-component-library: done  # 10 primitives to .d.ts contracts + Lucide icon vendoring/component + tests
  6-3-surface-reskin-and-vehicle-sheet: done  # re-skin 6 surfaces + VehicleSelector inline→bottom-sheet (ruled .decision-log.md:48): dialog semantics, focus trap, Esc, focus-return, Lucide gear. 3 adversarial reviews run clean 2026-06-16.
  epic-6-retrospective: done  # epic-6-retro-2026-06-17.md. Epic 6 deployed LIVE to gmaslist.com (deploy dep-d8p1610g4nts73ffkimg, commit ab4dd62). Action item: fix broken Render auto-deploy webhook (service made from public repo URL → no GitHub webhook).

  # Proprietary-data + AI-search strategy (no parent epic — tracked individually)
  # Source plan: investigations/ai-search-data-strategy-investigation.md (2026-06-28).
  cross-store-value-matcher: done  # cross-store-value-matcher.md — Phase 0 honesty hardening (honor existing weight-mismatch/unparseable flags as exclusions) + A1 keystone: same-SKU cross-store price-disparity engine (lowPrice/highPrice/spread%/storesCarrying). Private dataset/route only; additive read-only consumer of products.json. Downstream Phases 2-5 (Weedmaps wiring, accrual, legal gates→public SSR surfacing, monetization) named out-of-scope in the story. SHIPPED: PR #43 (squash 6cde2dd, ADR-062), hardened post-review by ADR-063 (6dea4a4, sold-out + numbered-strain fixes), deployed + VERIFIED LIVE; /api/value/disparities = 76.
  weedmaps-source-wiring: done  # weedmaps-source-wiring.md — AI-search Phase 2: static Weedmaps fetch → __NEXT_DATA__ → RawProduct → existing normalizeProduct/persist → existing matcher (zero matcher change). 4 defaults + private-registry + One-Hit-Wonder-dropped resolutions; 11 stores (3 overlap reuse ids + 8 net-new private/WA-geocoded); _weedmaps.ts + weedmaps-stores.ts + scrapeWeedmapsRun.ts + scrape-weedmaps.yml (schedule DISABLED). 41 new tests, server suite 357 green, build clean, matcher/deals untouched. SHIPPED: PR #44 MERGED + DEPLOYED 2026-06-29. OPERATIONAL GATE RESOLVED → BLOCKED: workflow_dispatch re-probe (run 28406848309) = HTTP 406 on 44/44 from datacenter IP; schedule stays disabled (PR #45 hardens header), Phase 3 accrual needs a residential-IP ingestion path.
  deal-sku-bridge: done  # deal-sku-bridge.md — GOAL 1 missing relation (deal→SKU), from data-layer roundtable audit 2026-06-30. Read-only THIRD consumer (mirrors crossStoreValue.ts) inferring deal SCOPE from free-text description → links banner to same-store SKUs (storewide / category+weight via canonicalWeightGrams). Honesty-gated: NO banner-% savings (fix6), observed specialPrice stays price-of-record, unsupported-category/brand/unresolved counted-not-fanned-out. Preserves ADR-043/053 Deal↔Product decoupling (no Deal/ProductsFile write-path change). Note: motive narrowed after code-grounding — observed specialPrice already delivers reconciled price, so value is scope+temporal linkage, not a new discount number. AC1-7 done (ADR-070); AC7 private route GET /api/value/deal-scope built per Erik option B 2026-07-01 (raw deals so temporal windows survive; fail-soft; no UI). PR #53. Flip to done on merge.

  # ================================================================
  # Derivation Engine (new product increment) — SEPARATE from the shipped Epics 1-6 above.
  # Source: planning-artifacts/epics-derivation-engine.md + prds/prd-Happy-2026-07-06/.
  # Epic keys are namespaced `epic-derivation-N` and stories `derivation-1-M-...` so they do
  # NOT collide with the shipped numeric epics/stories. Only Epic 1 (Tier-1 facts) is decomposed.
  # ================================================================

  # Derivation Epic 1: Honest daily relational facts, served
  epic-derivation-1: done  # ALL stories done 2026-07-10 (1.0–1.8 dev'd + reviewed; 1.9 verified already-satisfied by 1.0/ADR-077). Retro optional.
  products-storage-sqlite-phase-1: done  # Derivation Story 1.0 (ADR-077 Phase 1 substrate). HARD DEADLINE ~2026-07-23 (git 50MB wall). raw products.json → local SQLite on home machine; Render serves small derived facts only, NEVER queries home DB. Kills the file-size wall at $0 + creates the query substrate the Tier-1 facts are gated on. Golden-file parity test + intact-history migration are load-bearing ACs. Phase 0 (mtime-cache) + Phase 2 (hardening) = separate tracks. Story file: products-storage-sqlite-phase-1.md. Code review 2026-07-07: 10 patches fixed (incl. derivation fail-soft data-loss risk + missing AC8 deal-scope parity test), 6 deferred, 5 dismissed. 459 server tests green; build clean. Operational first-run (Erik's home machine) still pending.
  derivation-1-1-runner-served-artifact-envelope: done  # SCOPE NARROWED at story-creation (2026-07-07): runner/orchestration/routes already shipped early inside 1.0 — net-new scope is just the honesty-envelope wrapper {data,excluded,coverage,generatedAt} on the two already-served disparities/deal-scope artifacts. FR1/2/3/5/7. Decision E, Gate 1 match-key (already covered by existing tests, cited not duplicated). Implemented 2026-07-07: derivedEnvelope.ts + wired through runner/routes; 468 tests green, build clean. SHIPPED: PR #70 squash-merged → master `8c75a74`. Erik marked done as-is, no separate code-review pass.
  derivation-1-2-presence-aware-time-series-helper: done  # Gate 3 primitive (no-observation vs observed-unchanged); ships with 1.2.5 as first consumer. FR6. Story file: derivation-1-2-presence-aware-time-series-helper.md. Scope is the primitive ONLY (zero callers by design) — 1.2.5 wires the first real consumer as its own story. Implemented + reviewed 2026-07-07: presenceAwareSeries.ts + tests, 6 review patches applied, 480 server tests green, build clean.
  derivation-1-2-5-source-extraction-health-fact: done  # extraction-broke vs genuinely-empty; derivation-time, today-count vs trailing-median, decision C. Precondition for 1.7. Story-creation grounding (2026-07-09) against live products.db found caravan-cannabis-burlington/the-vault-silvana are BOTH zero-observations-ever (indistinguishable) — corrected to a 3rd status `insufficient-history`; live worked example for the collapse case is the 2020-solutions pair instead. Implemented 2026-07-09: extractionHealth.ts + wired into deriveFactsRun.ts; 490 tests green, build clean. Live-data proof confirmed both AC5/AC6 AND incidentally caught a real ~2-day Dutchie-scraper outage (16/26 stores flagged suspected today) — flagged to Erik, not fixed here. Code review 2026-07-09: 2 patches fixed (write-ordering risk gating pre-existing writes on the new fallible step; unsafe type cast), 2 deferred (wall-clock `today` anchoring — ops sequencing call; 14-day window under-reporting long-dark stores), 12 dismissed (mostly findings contradicting explicit spec decisions). 490 tests green, build clean.
  derivation-1-3-special-start-end-event-detection: done  # D1 / FR8. Special-start/end from sticky-list-price series. Gates 2,3. Story-creation grounding (2026-07-09) against live products.db confirmed the epics AC as written (no correction needed, unlike 1.2.5) — 2026-07-07 (673 starts) and 2026-06-25 (337 ends, matches cannazone-old-hwy-99 snap-back) confirmed exactly via live-data proof. Implemented 2026-07-09: specialEvents.ts + wired into deriveFactsRun.ts. Code review 2026-07-09: 1 decision resolved (fold future-dated-history products into gapCount rather than a new exclusion reason), 2 patches fixed (Gate-2 test strengthened to both event types, exact event-count assertion added), 1 deferred (pre-existing chronological-order assumption, inherited from 1.2/1.2.5), 11 dismissed. 502 tests green, build clean. Story file: derivation-1-3-special-start-end-event-detection.md.
  derivation-1-4-cross-store-disparity-rollups: done  # D4 / FR11. SEPARATE artifact disparity-rollups.json (decision A, keeps 1.1 oracle valid). Gate 1 match-key precision. Story-creation grounding (2026-07-09): live count is 246 (not 217, epics doc stale); brand scoped OUT (deferred to 1.6 per decision B); real geo-join gap found (7/21 stores only in private WEEDMAPS_STORES registry, not data.json) — rollup geo lookup merges both (missingGeoCount:0 live). New route /api/value/disparity-rollups added per FR11's own "served consumer surface" text. Code review 2026-07-09: 1 decision resolved (honest-metric fix — timesCheapest/timesPriciest gated on highPrice>lowPrice so zero-spread ties, 47% of rows, count toward neither; was inflating up to ~13×, e.g. 2020-solutions-pacific-highway 82→6), 2 dismissed. 519 tests green (+1 tie test), build clean.
  derivation-1-5-brand-discount-personas: done  # D2 / FR9. Type-gate F (flat banner-rate + potency unreachable, breach won't compile). Shared brand-normalizer added here (decision B). Story created 2026-07-09: grounded against live products.db — "typical discount depth" reframed to special-FREQUENCY persona (specialDayFraction) since magnitude is fix6-meaningless + Epic-2-gated (D6/FR13); 3 personas classified from observation.special boolean via 1.2 helper (always≥0.95 / never≤0.05 / intermittent); shared normalizeBrandKey in new brandKey.ts (10 live casing/punct collisions merged, 516→505 brands); 826 null-brand products excluded+counted; internal-only, NO route (mirrors extraction-health/special-events). Live proof: 118 always / 39 never / 134 intermittent / 214 insufficient-history.
  derivation-1-6-brand-store-matrix: done  # D5 / FR12. Cross-sectional grouping; consumes brand-normalizer from 1.5. Type-gate F. Story created 2026-07-10: grounded vs live products.db (505 brands, 225 multi-store, 201 like-for-like cheapest cells of 4,438). CORE honesty split — availability (brand→stores→tiers) is pure grouping; "cheapest" is a same-product+same-weight Gate-1 claim only (≥2 stores), brand grouping alone never crowns a winner. Decision-F input looser than 1.5 (single reduced specialPrice??basePrice allowed for cheapest; price-PAIR/rate + potency stay non-compilable). Cross-sectional (latest obs, no 1.2 helper/today); reuses deriveMatchKey/canonicalWeightGrams/normalizeBrandKey unchanged. No route (mirrors 1.5).
  derivation-1-7-new-arrival-dormancy-feed: done  # D3 / FR10. Gate 4 missed-scrape-ambiguity flag; consumes 1.2.5 so a silent hole isn't read as delisting. Last fact (most dangerous). Story created 2026-07-10: grounded vs live products.db which has an ACTIVE ~2-day Dutchie outage (12 stores zero-today) — naive dormancy would false-fire ~2,765 SKUs as delisted, making the store-health gate (only `ok` stores emit dormancy) load-bearing. Dormancy floor = absent from store's ≥2 most-recent scrape-runs (grounded: 29.4% of 1-run absences reappear next day), counted in runs not calendar days; every dormant entry carries missedScrapeAmbiguity:true (never a delisting claim). New-arrival = first-ever obs today, store-onboarding-guarded. Per-SKU gap-tolerant presence fact mirroring specialEvents.ts + 1.2 helper; first real consumer of buildExtractionHealthReport. Presence-only output (no price/potency). No route (mirrors 1.5/1.6).
  derivation-1-8-derivation-run-freshness-health-alerting: done  # extends existing evaluateAlert path (decision D); reads generatedAt off the envelope. Story created 2026-07-10: grounded vs live site — all 8 facts stamped within ~0.6s of the same daily derive run (checking the served disparities envelope's generatedAt is a faithful whole-run proxy; live floor 2026-07-10T18:02:47.401Z). Additive sibling pure fn `evaluateRunFreshness` in alertGate.ts (NOT a new module — decision D), reads generatedAt off the honesty envelope generically; daily-cadence windows (fresh ~28h / stale-alert ~50h → 2 missed days reds, 1 late run doesn't); epoch/never-derived sentinel (EMPTY_GENERATED_AT) → alert. Wired into existing hourly alert-gate job via new FRESHNESS_URL env (no new schedule/system). 3 open Qs RESOLVED by Erik (defaults ratified; extraction-health alerting deferred). Code review 2026-07-10: 1 decision resolved (future-skew plausibility cap ADDED — DERIVE_MAX_FUTURE_SKEW_MS 6h + futureSkew flag, Erik's call), 5 patches applied (no early-exit on freshness fetch failure so store ALERT lines still print; fresh-band field consumes freshWindowMs; distinct envelope-shape diagnostic; stale-boundary test), 1 deferred (fetch retry → deferred-work.md), 9 dismissed. 567 tests green, build clean, live-proofed. No route/client/data.json change.
  derivation-1-9-remove-api-products: done  # FR4. VERIFIED ALREADY-SATISFIED by 1.0/ADR-077 (2026-07-10, no dev needed): route + test were removed during story 1.0 (ADR-077 open-decision #2 "dropped entirely"); grep-clean gate passes (only 2 comments DOCUMENTING the removal remain: server/index.ts:35, valueRoute.test.ts:20); live GET gmaslist.com/api/products → 404. The remove-now-vs-defer fork dissolved — removal had already happened. No story file created (nothing to dev).
  epic-derivation-1-retrospective: done  # epic-derivation-1-retro-2026-07-10.md — ratified by Erik; action items 1-2 (happy-time-mt-vernon stale + Dutchie outage root-cause) run PARALLEL to Epic 2, not blocking dev.

  # Derivation Epic 2: Accrual facts (the honest discount + delivered/regional value)
  # Decomposed 2026-07-10 (epics-derivation-engine.md, Epic 2 — Stories). FR16 = cross-cutting
  # type-gate AC on every story (Erik's ruling, no dedicated FR16 story). Data-integrity retro
  # items (happy-time-mt-vernon stale, Dutchie outage root-cause) run PARALLEL, non-blocking.
  epic-derivation-2: done
  derivation-2-1-price-vs-own-rolling-median: done  # D6 / FR13, fix6 keystone. Code-reviewed 2026-07-12 (3-layer bmad-code-review): 7/7 ACs pass; 4 patches applied (same-day time-latest tie-break + test, integration-test UTC-midnight anchor, test hygiene, envelope-unit docs); 1 deferred (UTC-day seam → deferred-work.md); 653 server tests green, build clean. New SQLite time-range query (first real consumer of (product_key, observedAt) indices); 1.2 helper; min-obs floor in DISTINCT CALENDAR DAYS; history sorted on read; window/floor = named constants Erik ratifies at dev-start (~16 days history → ships suppression-heavy by design). Gate 2 + FR16 type-gate. Story created 2026-07-11: grounded vs live products.db (18-day span, 6,143 products) — at floor 7, 3,055 option-series / 2,058 comparable latest-day → 207 below own median (median mover −19.1%) / 1,830 at median / 21 above; series key = (product, option label verbatim) NOT canonical weight (no $/g claim → no mg-parse hazard); effective price = specialPrice ?? basePrice single-reduced at runner boundary; proposed ROLLING_WINDOW_DAYS=30 / MIN_OBSERVED_DAYS=7 / movers-only emit await Erik's dev-start ratification.
  derivation-2-2-cheapest-delivered-input-fact: done# D7 / FR14. Per match-key specialPrice ?? basePrice + store lat/lng; NO baked-in origin (ADR-057) — delivered cost composes user-relatively downstream via existing roundTripGasCost (verified reusable). Gate 1 ≥2-store cells + WEIGHT_BASED_CATEGORIES/EXCLUDED_FLAGS mg-parse gating + FR16 type-gate.
  derivation-2-3-regional-price-floor-availability-gap: done  # D8 / FR15. Code-reviewed 2026-07-13 (3-layer bmad-code-review): all 5 ACs + bound decisions met; 3 patches applied (module-wide localeCompare collation consistency; Number.isFinite geo guard vs NaN/Infinity phantom singletons; fully-stocked-cluster empty-gaps test — literal empty-universe+cluster case proven unreachable); 1 deferred (oracle-wide stale-latest-obs price via suspected store — pre-existing, affects all disparity facts); 10 dismissed. 678 server tests green (+2), real build clean. DONE dev 2026-07-12 (bmad-dev-story, ADR-086): pure regionalPriceFloor.ts (3rd sibling of 1.4/2.2) — union-find single-linkage clustering (local haversine R=3958.8, no road factor; CLUSTER_RADIUS_MILES=10 Erik-ratified), floors = min WITHIN Disparity cell (Gate 1 inherited, ties listed, single-store floors emitted+counted), gaps = data-derived universe minus per-cluster presence (suspected-store suppression 1.7-pattern, insufficient-history never suppresses), null/absent-geo unclustered+counted. Runner reuses in-scope disparities+geoLookup+storeStatus (no rebuild), presence projected {dispensaryId,category} (decision F/FR16), written LAST + $derivedFiles append. 14 pure-fn + 2 runner integration tests; server 676/676 green, real build clean. Live proof over committed disparities.json: 4 clusters (1/5/5/11), kaleafa-oak-harbor single-store, 432 floors reconcile byte-for-byte (0 mismatches), derived dir byte-identical. Fixed a pre-written centroid-fixture bug (diagonal ≈10.10mi just outside radius yet asserting merge; lng +0.1→+0.05).
  epic-derivation-2-retrospective: done  # epic-derivation-2-retro-2026-07-13.md — ratified by Erik. Decisions: Epic 3 = close 3-1 then in-app value cards; oracle-freshness (mt-vernon stale + Dutchie outage + 2.3 stale-floor finding) PROMOTED to one named parallel story (freshness gate at buildMatchReport, affects all disparity-derived facts).

  # Derivation Epic 3 — partially sliced (2026-07-10, bmad-quick-dev). ADR-066 legal gate is CLOSED;
  # the SEO comparison-pages slice runs on shipped Epic 1 disparity facts (no Epic 2 dependency).
  # In-app value cards remain gated on Epic 2 FR13 → deferred (deferred-work.md).
  epic-derivation-3: done  # Feed the surfaces: AI-search/SEO comparison pages + in-app value cards consuming the derived facts. ALL 3 slices done 2026-07-13: 3-1 SEO /compare pages, 3-2 in-app "Real price drops" (standalone), 3-3 relocate drops INTO each store's card (reopened per Erik feedback, ratified UX spec real-price-drops-in-card.md; 3-layer code review, ADR-088). CLOSED 2026-07-13. Retro optional.
  derivation-3-2-in-app-value-cards: done  # CODE-REVIEWED 2026-07-13 (3-layer bmad-code-review): Auditor all 7 ACs pass; 4 patches applied (suppress 0%-display rows [Erik decision], aria double-announce fix, empty-productId React-key reject, empty-option separator), 10 dismissed; client 584 + server 682 green, real build clean. DEV COMPLETE 2026-07-13 (bmad-dev-story, ADR-087): flagship-only MVP shipped — new served route GET /api/value/price-vs-own-median (fail-soft mirror of disparitiesRoute) + useValueDrops hook (selectDrops filters to pctVsMedian<0 so a premium never shows as a drop) + ValueDrops.tsx "Real price drops" section per ratified UX spec (real-price-drops-surface.md), wired between distance slider & store feed via a 1-line additive DealFeed edit (DealCard/useDeals/data.json untouched). Honesty: no banner % (Gate 2), no verdict (ADR-009), store-join drops absent/failed/stale rows, whole-# %/2dp $ in single amber hue. price-vs-own-median.json NOT yet committed (home DB empty 2026-07-12) → empty-envelope fallback serves the gap, republishes next derive; both live paths smoke-tested. Deviation flagged for Erik: count = rendered rows (stricter than spec's belowMedianCount). Client 581 + server 682 green, real build clean. Ready for code-review (recommend a different LLM). --- ORIGINAL CREATE-STORY NOTE: created 2026-07-13 (create-story). DEV-START 2026-07-13: Erik ratified scope = flagship-only MVP (price-vs-own-median), standalone "Real price drops" section at TOP of feed (Option B); ACs stand. Erik requested a UX pass (Sally/bmad-ux) BEFORE build — dev paused pending value-card layout spec. Epic-3 slice 2, unblocked now Epic 2 (FR13 price-vs-own-median) shipped. Story file: derivation-3-2-in-app-value-cards.md. RECOMMENDED MVP = flagship "real price drop" surface over price-vs-own-median (the only honest discount, Gate 2). Two load-bearing DEV-START questions for Erik: (1) which card(s) — recommend flagship-only, defer cheapest-delivered/brand-matrix/regional-floor; (2) surface shape — recommend standalone "Real price drops" section (Option B), NOT a per-DealCard badge (deals are store-keyed, fact is product-keyed; no honest banner→SKU savings join). Load-bearing findings: price-vs-own-median has NO served route yet (only disparities/deal-scope/rollups on /api/value/*) → adding GET /api/value/price-vs-own-median is in-scope; price-vs-own-median.json/cheapest-delivered.json/regional-price-floor.json NOT currently committed (home products.db empty 2026-07-12 — verify feeders repopulated). No UX spec for value cards (gap flagged). Additive/fail-soft; DealFeed/DealCard unchanged.
  derivation-3-3-price-drops-in-card: done  # CODE-REVIEWED 2026-07-13 (3-layer bmad-code-review): Auditor all 8 ACs pass; 2 decisions resolved (D1 → patch: dropped the per-card ARIA `region` landmark so N cards no longer flood landmark nav — `<section aria-labelledby>`→`<div>`+`<h3>` heading nav, realigns with UX spec §Accessibility; D2 → accepted as-is 2a: drops NOT category-scoped on carded stores — `drop.category` product taxonomy `'Flower'`/`'Concentrate'` has no clean bridge to the `DealCategory` glyph vocab, would need its own spec), 1 extra patch (stale CSS comments), 1 deferred (sub-1% rounds up to "1%" — pre-existing/spec-compliant, in deferred-work.md), 3 dismissed. Client 594 green, real build clean. --- Relocated the "Real price drops" surface OUT of the standalone top-of-feed block and INTO each store's DealCard (labeled strip under that store's deals). Ratified UX spec: real-price-drops-in-card.md (Sally, Erik-ratified). Two ratified decisions: drops follow their store on the distance filter (hidden with the store past the slider); union/no-drop-lost (a fresh in-range store with a real drop but no banner deal GETS a card so no drop is lost). Copy/fact/honesty-gates inherited verbatim from 3-2. Story file: derivation-3-3-price-drops-in-card.md.
  # Cross-cutting oracle-freshness story — promoted from the epic-derivation-2 retro (2026-07-13,
  # action item 3); Layer-2 gap #1 of ADR-095. Parallel to Epic 3, engine-wide (hits every
  # disparity-derived fact at once). Absorbs Epic-1 retro items 1-2 (mt-vernon stale, Dutchie outage).
  oracle-freshness-gate: done  # SHIPPED 2026-07-22 — merged as PR #98 / commit e3cc63f "feat(derivation): oracle freshness gate (Gate 6) at buildMatchReport". Implemented + 3-layer bmad-code-review PASSED (Opus 4.8). Gate 6 at buildMatchReport: anchor = global max observed day (data-derived, seam-proof), FRESHNESS_MAX_LAG_DAYS=1, per-record, excluded+counted (staleRecords + `stale` envelope reason); siblings inherit free. Review applied 1 decision + 3 patches (future-day anchor guard, calendar-invalid-date rejection, maxLagDays/anchor input validation) + 6 hardening tests → 721 server green, build clean, ADR-096. AC7 PROVEN on live home DB 2026-07-22 (read-only instrumentation over ~/GmaS-data/products.db, no push): gate fires — anchor 2026-07-22, staleThreshold 2026-07-21, 2,490 records excluded stale, 287 disparities suppressed (1,430 gated vs 1,717 ungated), per-store stale sum = 2,490 exactly. Store-level mt-vernon fall-out NOT reproducible today (mt-vernon fresh, latest obs 2026-07-22 — only 75 of its SKUs individually stale, store kept); per-record gate confirmed working, July-21 whole-store staleness was a pre-scrape snapshot now resolved. 4 findings deferred → deferred-work.md. Original: freshness gate at buildMatchReport so a stale prior-day price can't set a cross-store low / inflate spread / become a regional floor. Grounded 2026-07-21 vs committed products.db: 1,719/5,219 products (32.9%) have a stale latest obs (before global max day 2026-07-06) — structural, not just outage stores; catches silently-vanished SKUs Gate 4 (sold-out) can't see. Per-record gate at crossStoreValue.ts:96, additive Gate 6, excluded+counted (new `staleRecords` + `stale` envelope reason). Siblings cheapest-delivered/regional-floor/rollups inherit free (consume report.disparities); price-vs-own-median OUT (own windowed reader). LOAD-BEARING DEV-START Qs for Erik: (1) anchor = wall-clock UTC today [seam footgun] vs GLOBAL max observed day [recommended, seam-proof — NOT per-store]; (2) FRESHNESS_MAX_LAG_DAYS 0/1 ratify; (3) per-record [recommended] vs per-store; (4) signature/compat (3 test call sites; runner always passes anchor so gate never silently off). NEEDS Erik go-ahead + anchor ratification to BUILD (create-story only planned it).
  # Cross-cutting SEO/reach follow-up (no parent epic) — from the 2026-07-29 Semrush audit
  # (investigations/semrush-site-audit-investigation.md). Region-category title trim already
  # shipped (ADR-110). This is the remaining "21 low word count" item = the 18 /store pages.
  geo-page-sitemap-durability: done  # SHIPPED 2026-07-31 (pushed 37b3a7d → Render auto-deploy; rebased over automated data refreshes; real client+server builds clean pre-push). DEV COMPLETE (bmad-dev-story, ADR-111): staleness guard no longer drops all-dark floors — it marks them stale — so region.categories (→ sitemap URL + route existence) is now a pure function of the STABLE committed floors (freshness-invariant); render keeps last-known rows + "freshness unverified" caveat (Ruling A(b)); empty-state only for a genuinely-empty committed category (structural absence still 404s, AC2). Added RegionalFloor.stale?; slug-stability CI guard (regionSlugStability.test.ts pins bellingham/everett/mount-vernon; on-drift redirect deferred). Does NOT touch derivation/storeStatus thresholds/Gate 6 (ADR-096). 858/858 server green, client+server real builds clean. NEXT: code-review (recommend a different LLM) then push/deploy (Erik standing go-ahead). --- Story created 2026-07-31 (bmad-create-story) from investigations/geo-page-volatility-investigation.md (High-confidence root cause). Geo /compare/<cat>/<region> + /compare/<region> URLs transiently 404 + drop from sitemap when a cluster's stores go stale, because region.categories (URL/sitemap existence) is built from the POST-staleness-guard floor set (regionModel.ts:157-161) — the stable committed price-floor data (7/7 days full 4-cat structure) is re-projected against a request-time 3h freshness overlay (storeStatus.ts + buildApiData) with zero hysteresis. Observed 19→14 /compare/* on the 2026-07-30 19:23 redeploy, recovered to 19 next pull (transient, NOT thin data). FIX = decouple URL existence from freshness: category-set from committed floors (URL never 404s on staleness), keep the freshness filter ONLY on rendered rows/copy (existing empty-state at compareRoute.ts:620-623 is the honest fallback; recommended DEV-START Ruling A(b) = keep last-known rows + "as of" caveat). + slug-stability CI test for the latent dominant-city-flip risk (Hyp1: refuted-as-active, slug is a fn of committed membership only). Structural absence still 404s (AC2). Does NOT touch the derivation or the engine-wide Gate 6 (ADR-096/oracle-freshness-gate). NEEDS Erik go-ahead to BUILD + 2 DEV-START rulings (render posture A; guard-is-test-only B).
  store-page-crawlable-enrichment: ready-for-dev  # ON HOLD as a word-count fix (2026-07-29 GSC pull). Honesty-safe evergreen fact prose on the 18 /store pages; story file stands. But GSC index-status evidence came back and REFUTES the "thin hurts indexing" premise: Google DISCOVERED the 40 sitemap URLs (read 7/29, Success) but has NOT crawled the store pages — URL-inspection of /store/remedy-tulalip = "Discovered, currently not indexed / Last crawl N/A" (never fetched). No "crawled-not-indexed" or "discovered-not-indexed" THIN bucket exists; the only not-indexed reason is "Page with redirect" = the homepage's 3 canonical redirect twins. 0 web-search clicks. => the binding constraint is CRAWL/DISCOVERY (brand-new low-authority property), NOT word count; a page Google never fetched can't be thin-rejected. DON'T build this to clear an inert Semrush warning. Enrichment retains value as citation QUALITY once pages are actually crawled — revisit then. AI-citation (Phase-0 monitor, 0/8) is the real north star + crawls independently of Google. Full evidence: investigations/semrush-site-audit-investigation.md "Follow-up 2026-07-29 (GSC)".

  derivation-3-1-seo-comparison-pages: done  # code-reviewed 2026-07-13 (3-layer bmad-code-review): all 5 ACs + honesty gates MET; 3 patches applied (element-shape fail-soft guard vs malformed-row 500; omit epoch dateModified from Dataset JSON-LD; relax honesty test to discount-phrasing only per Erik — product-name % allowed); 3 deferred (slug collision, category &<> verbatim-match, rollups/disparities independent-read desync — all dormant/narrow); 4 dismissed. 678 server tests green, real build clean. spec-derivation-3-1-seo-comparison-pages.md — server-rendered /compare index + per-category pages over disparities.json/disparity-rollups.json (Dataset JSON-LD, mirrors ADR-078 /about SSR; Dataset schema NOT commerce schema; stable-entity URLs only). Honesty gates 1/2/5 + facts-not-menus. In-app value cards deferred (Epic-2-gated).
