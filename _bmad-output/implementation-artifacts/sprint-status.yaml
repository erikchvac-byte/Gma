# generated: 2026-06-09
# last_updated: 2026-06-09 (story 1-3 done)
# project: Happy
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-06-09
last_updated: 2026-07-07 (derivation-1-1-runner-served-artifact-envelope → done)
project: Happy
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # Epic 1: Project Foundation & Data Layer
  epic-1: done
  1-1-project-scaffold: done
  1-2-seed-dispensary-data: done
  1-3-get-api-data-endpoint-with-active-deal-filtering: done
  epic-1-retrospective: optional

  # Epic 2: Core Deal Experience
  epic-2: done
  2-1-age-gate: done
  2-2-deal-feed-with-active-deals-and-empty-state: done
  2-3-deal-cards: done
  2-4-gas-cost-calculation: done
  2-5-distance-filter: done
  2-6-stale-source-indicator: done
  epic-2-retrospective: optional

  # Epic 3: Gas Cost Accuracy & Personalization
  epic-3: done
  3-1-eia-gas-price-refresh: done
  3-2-vehicle-precision-mode: done
  epic-3-retrospective: optional

  # Epic 4: Live Deal Data via Scraper
  epic-4: done
  4-1-scraper-engine-and-orchestrator: done
  4-2-first-dispensary-html-parsers: done  # re-scoped 2026-06-13 to plain-HTML only (remedy-tulalip); the-joint + jet + kush21 are Dutchie → 4.3
  4-3-dutchie-iframe-dispensary-support: done
  epic-4-retrospective: done

  # Cross-cutting follow-up stories (no parent epic — tracked individually)
  # Promoted from the Epic 4 retrospective (action item 1, Erik 2026-06-13).
  data-hardening: done  # spec-data-hardening.md — client useDeals boundary validation + scraper-ingestion deal normalization
  4-3-live-pass: done  # spec-4-3-live-pass — live E2E: 3/3 Dutchie stores flow real deals; transform reconciled to real shape (ADR-030); all 4 sources data-complete
  5-1-deploy-scraper-service: done  # deploy-READINESS + ADR only (Erik: stay free / defer live 2026-06-15) — env-parametrize SCRAPER_URL + topology ADR-033 + runbook; no Render spend. Review-verified 2026-06-16 (ADR reconcile confirmed).
  compliance-launch-gate: done  # compliance-launch-gate.md — the 3 "verify with counsel" items from regulated-content review: (1) description sanitization+blocklist at normalizeDeals chokepoint [WAC 314-55-155 advertiser-status posture], (2) standing disclaimer footer + flag-gated WA-warning slot, (3) age-gate decline affordance. Two-track: counsel determination (Erik) + defensible engineering posture (dev). LAUNCH-GATING.
  commit-back-seed: done  # commit-back-seed.md (Correct-Course 2026-06-22, ADR-047) — make Render's ephemeral-disk reset non-destructive: cron commits scraped data.json back to master as a fresh seed, built via in-CI applyIngest merge (NOT GET /api/data, which filterActiveDeals makes lossy). New commit-back job in scrape-ingest.yml + commitBackSeed.ts + ingestRun emit flag; paths-ignore loop-breaker. Fixes frozen "Last updated Jun 14" / 17-unavailable flip-flop. Out of scope: lowering the unresolved-Dutchie-cName floor.

  # Epic 6: Apply Gma's Helper Design System to the client
  # Split ruled with Erik 2026-06-16: foundation → primitives → surface re-skin.
  # Source of truth: planning-artifacts/ux-designs/ux-Happy-2026-06-11/{DESIGN.md,EXPERIENCE.md} + imports/gmas-helper-design-system.
  epic-6: done
  6-1-design-system-foundation: done  # token layer + Tailwind v4 @theme + self-hosted fonts + base.css; no component/surface changes. Reviewed/cleared 2026-06-16 (status-drift fix).
  6-2-primitive-component-library: done  # 10 primitives to .d.ts contracts + Lucide icon vendoring/component + tests
  6-3-surface-reskin-and-vehicle-sheet: done  # re-skin 6 surfaces + VehicleSelector inline→bottom-sheet (ruled .decision-log.md:48): dialog semantics, focus trap, Esc, focus-return, Lucide gear. 3 adversarial reviews run clean 2026-06-16.
  epic-6-retrospective: done  # epic-6-retro-2026-06-17.md. Epic 6 deployed LIVE to gmaslist.com (deploy dep-d8p1610g4nts73ffkimg, commit ab4dd62). Action item: fix broken Render auto-deploy webhook (service made from public repo URL → no GitHub webhook).

  # Proprietary-data + AI-search strategy (no parent epic — tracked individually)
  # Source plan: investigations/ai-search-data-strategy-investigation.md (2026-06-28).
  cross-store-value-matcher: done  # cross-store-value-matcher.md — Phase 0 honesty hardening (honor existing weight-mismatch/unparseable flags as exclusions) + A1 keystone: same-SKU cross-store price-disparity engine (lowPrice/highPrice/spread%/storesCarrying). Private dataset/route only; additive read-only consumer of products.json. Downstream Phases 2-5 (Weedmaps wiring, accrual, legal gates→public SSR surfacing, monetization) named out-of-scope in the story. SHIPPED: PR #43 (squash 6cde2dd, ADR-062), hardened post-review by ADR-063 (6dea4a4, sold-out + numbered-strain fixes), deployed + VERIFIED LIVE; /api/value/disparities = 76.
  weedmaps-source-wiring: done  # weedmaps-source-wiring.md — AI-search Phase 2: static Weedmaps fetch → __NEXT_DATA__ → RawProduct → existing normalizeProduct/persist → existing matcher (zero matcher change). 4 defaults + private-registry + One-Hit-Wonder-dropped resolutions; 11 stores (3 overlap reuse ids + 8 net-new private/WA-geocoded); _weedmaps.ts + weedmaps-stores.ts + scrapeWeedmapsRun.ts + scrape-weedmaps.yml (schedule DISABLED). 41 new tests, server suite 357 green, build clean, matcher/deals untouched. SHIPPED: PR #44 MERGED + DEPLOYED 2026-06-29. OPERATIONAL GATE RESOLVED → BLOCKED: workflow_dispatch re-probe (run 28406848309) = HTTP 406 on 44/44 from datacenter IP; schedule stays disabled (PR #45 hardens header), Phase 3 accrual needs a residential-IP ingestion path.
  deal-sku-bridge: done  # deal-sku-bridge.md — GOAL 1 missing relation (deal→SKU), from data-layer roundtable audit 2026-06-30. Read-only THIRD consumer (mirrors crossStoreValue.ts) inferring deal SCOPE from free-text description → links banner to same-store SKUs (storewide / category+weight via canonicalWeightGrams). Honesty-gated: NO banner-% savings (fix6), observed specialPrice stays price-of-record, unsupported-category/brand/unresolved counted-not-fanned-out. Preserves ADR-043/053 Deal↔Product decoupling (no Deal/ProductsFile write-path change). Note: motive narrowed after code-grounding — observed specialPrice already delivers reconciled price, so value is scope+temporal linkage, not a new discount number. AC1-7 done (ADR-070); AC7 private route GET /api/value/deal-scope built per Erik option B 2026-07-01 (raw deals so temporal windows survive; fail-soft; no UI). PR #53. Flip to done on merge.

  # ================================================================
  # Derivation Engine (new product increment) — SEPARATE from the shipped Epics 1-6 above.
  # Source: planning-artifacts/epics-derivation-engine.md + prds/prd-Happy-2026-07-06/.
  # Epic keys are namespaced `epic-derivation-N` and stories `derivation-1-M-...` so they do
  # NOT collide with the shipped numeric epics/stories. Only Epic 1 (Tier-1 facts) is decomposed.
  # ================================================================

  # Derivation Epic 1: Honest daily relational facts, served
  epic-derivation-1: in-progress  # first story (1.0) exists → in-progress
  products-storage-sqlite-phase-1: done  # Derivation Story 1.0 (ADR-077 Phase 1 substrate). HARD DEADLINE ~2026-07-23 (git 50MB wall). raw products.json → local SQLite on home machine; Render serves small derived facts only, NEVER queries home DB. Kills the file-size wall at $0 + creates the query substrate the Tier-1 facts are gated on. Golden-file parity test + intact-history migration are load-bearing ACs. Phase 0 (mtime-cache) + Phase 2 (hardening) = separate tracks. Story file: products-storage-sqlite-phase-1.md. Code review 2026-07-07: 10 patches fixed (incl. derivation fail-soft data-loss risk + missing AC8 deal-scope parity test), 6 deferred, 5 dismissed. 459 server tests green; build clean. Operational first-run (Erik's home machine) still pending.
  derivation-1-1-runner-served-artifact-envelope: done  # SCOPE NARROWED at story-creation (2026-07-07): runner/orchestration/routes already shipped early inside 1.0 — net-new scope is just the honesty-envelope wrapper {data,excluded,coverage,generatedAt} on the two already-served disparities/deal-scope artifacts. FR1/2/3/5/7. Decision E, Gate 1 match-key (already covered by existing tests, cited not duplicated). Implemented 2026-07-07: derivedEnvelope.ts + wired through runner/routes; 468 tests green, build clean. SHIPPED: PR #70 squash-merged → master `8c75a74`. Erik marked done as-is, no separate code-review pass.
  derivation-1-2-presence-aware-time-series-helper: backlog  # Gate 3 primitive (no-observation vs observed-unchanged); ships with 1.2.5 as first consumer. FR6.
  derivation-1-2-5-source-extraction-health-fact: backlog  # extraction-broke vs genuinely-empty (caravan-cannabis-burlington is the live proof); derivation-time, today-count vs trailing-median, decision C. Precondition for 1.7.
  derivation-1-3-special-start-end-event-detection: backlog  # D1 / FR8. Special-start/end from sticky-list-price series. Gates 2,3.
  derivation-1-4-cross-store-disparity-rollups: backlog  # D4 / FR11. SEPARATE artifact disparity-rollups.json (decision A, keeps 1.1 oracle valid). Gate 1 match-key precision.
  derivation-1-5-brand-discount-personas: backlog  # D2 / FR9. Type-gate F (flat banner-rate + potency unreachable, breach won't compile). Shared brand-normalizer added here (decision B).
  derivation-1-6-brand-store-matrix: backlog  # D5 / FR12. Cross-sectional grouping; consumes brand-normalizer from 1.5. Type-gate F.
  derivation-1-7-new-arrival-dormancy-feed: backlog  # D3 / FR10. Gate 4 missed-scrape-ambiguity flag; consumes 1.2.5 so a silent hole isn't read as delisting. Last fact (most dangerous).
  derivation-1-8-derivation-run-freshness-health-alerting: backlog  # extends existing evaluateAlert path (decision D); reads generatedAt off the envelope.
  derivation-1-9-remove-api-products: backlog  # FR4. OPEN FORK: remove-now behind grep-clean gate vs defer→Epic 3. ACs written for remove-now (client/ already zero refs).
  epic-derivation-1-retrospective: optional

  # Derivation Epics 2-3 — NOT decomposed this run (listed for context; stories/retros added when decomposed).
  epic-derivation-2: backlog  # Tier-2 accrual facts: price vs own rolling median (honest discount, reopens fix6), cheapest-delivered, regional floor.
  epic-derivation-3: backlog  # Feed the surfaces: AI-search/SEO comparison pages + in-app value cards consuming the derived facts.
