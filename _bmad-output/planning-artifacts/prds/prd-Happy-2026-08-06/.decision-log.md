# Decision Log — Backlink Measure-and-Surface Tooling PRD

Canonical memory and audit trail. Every decision, change, and override is recorded here as the conversation unfolds.

## 2026-08-06

- Intent: **Create** (no prior PRD on this topic; existing PRDs prd-Happy-2026-06-08 and prd-Happy-2026-07-06 are both `final` and unrelated).
- Scope named by Erik: PRD covering all 4 backlink tools — (1) citation-share tracker extending the existing citation monitor, (2) unlinked-mention finder, (3) opportunity finder, (4) citation-ready fact packager. All strictly the "safe MEASURE & SURFACE" side; no automated placement.
- Primary input: backlink brief at `C:\Users\erikc\Dev\Happy Hunter Seeker\backlink-tooling-brief.md` (read in full by parent).
- Artifacts live in the `Happy` repo, not the Hunter Seeker folder.
- Working mode: **Fast path**. Stakes: internal tooling for solo operator (Erik) — lean PRD, FR-focused. Brain dump: nothing to add beyond the brief.
- **Sequencing (Erik):** Extend-first / cost order — 1) citation-share tracker, 2) fact packager, 3) opportunity finder, 4) unlinked-mention finder.
- **Data source (Erik):** Free-tier only — GSC Links (manual export), the existing Haiku + web_search stack, Perplexity Sonar. NO paid backlink API. Paid API noted as a deferred phase-2 option.
- **Output form (Erik):** Local reports (JSON + markdown), same pattern as the citation monitor; local-only, out of the server runtime. Human does the actual outreach.
- **Cadence (Erik):** Weekly scheduled + on-demand — citation-share tracker + unlinked-mention finder run weekly on the existing citation-monitor Scheduled Task; opportunity finder + fact packager run on demand.
- Mechanism/tech-how (extend `citationMonitor.ts`, write to `server/data/derived/*.json`, Scheduled Task wiring) captured in `addendum.md` — kept out of the capability-level PRD body.

## Finalize (2026-08-06)

- **Decision-log audit:** every decision above lands in the PRD (§4/§6/§10) or `addendum.md`. Clean.
- **Input reconciliation:** brief reconciled in `reconcile-brief.md` — no material gap; the brief's "on-page levers already pulled" is a non-gap (outside these tools' scope). Perplexity-second-engine reconciled forward.
- **Reviewer pass:** rubric walk in `review-rubric.md` — gate verdict PASS. Strong across all seven dimensions; two **low**, non-blocking findings (SM-2 attribution is a manual judgment call; FR-1 seed-set size confirm-at-build-time). Both deferred, neither a PRD defect. Reviewer run inline (finalize_reviewers empty; solo/internal stakes — no subagent fan-out).
- **Triage:** 4 Open Questions all non-phase-blockers (none make the PRD unsafe for epics/stories) → deferred, owner Erik/build-time, revisit conditions stated in §8. All 5 `[ASSUMPTION]` tags confirmed by Erik ("all is good"). `[NOTE FOR PM]` (paid backlink API) deferred to phase 2.
- **Polish:** doc is clean; light self-pass. The two editorial doc-standard skills were not separately spawned — stakes-calibrated call for a lean internal PRD.
- **Close:** `prd.md` status → `final`. Next: `bmad-create-epics-and-stories`.
