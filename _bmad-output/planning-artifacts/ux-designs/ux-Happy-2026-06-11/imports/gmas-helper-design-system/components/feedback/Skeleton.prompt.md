**Skeleton** / **SkeletonFeed** — pulsing loading placeholders. `SkeletonFeed` reproduces the deal feed's three-row loading state.

```jsx
<SkeletonFeed rows={3} />
<Skeleton width={120} height={20} />
```

Respects `prefers-reduced-motion` (no pulse).
