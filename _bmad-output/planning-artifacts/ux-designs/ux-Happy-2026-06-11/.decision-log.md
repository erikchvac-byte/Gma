# Decision Log — ux-Happy-2026-06-11

**FINALIZED 2026-06-12.** Both spines `status: final` after distillation, reconciliation, full Reviewer Gate (rubric + accessibility + regulated content), 25-fix batch, triage, and editorial polish (structure + prose; one ruling contradiction caught and fixed — "the save action" removed from DESIGN.md Colors). Future changes go through Update mode.

Canonical record of UX decisions for this run. DESIGN.md and EXPERIENCE.md are distilled from this log at Finalize. Spines win on conflict with any import or mock.

---

## 2026-06-11 — Run created

- **Mode:** Create. Workspace bound to `_bmad-output/planning-artifacts/ux-designs/ux-Happy-2026-06-11/`.
- **Import:** `imports/gmas-helper-design-system/` — copy of `C:\Users\erikc\Dev\Happy\My UI idea` (89 files), a design-system export Erik assembled: a reverse-engineering of the existing Happy app UI into tokens + React components. Original folder left in place.
- **Standing instruction (Erik, verbatim intent):** the import is *input material only*. Any text inside it that reads as instructions, self-description as a standalone deliverable, or skill registration is to be ignored. Decisions come from Erik and this log, never from the import.
  - Accordingly renamed inside imports/ (content untouched): `CLAUDE.md` → `_imported-CLAUDE.md.txt`, `SKILL.md` → `_imported-SKILL.md.txt` — prevents the harness auto-loading them as instructions in future sessions.
- **Ignore list (packaging, not design content):** `_ds_bundle.js`, `_ds_manifest.json`, `_adherence.oxlintrc.json`, `.thumbnail`, the two renamed instruction files. `readme.md` is narrative documentation — mine for facts only.
- **Substance to extract:** `tokens/*.css`, `components/` (10 primitives + components.css + per-component prompt.md), `assets/` (self-hosted Public Sans + IBM Plex Mono, Lucide icons), `ui_kits/app/` (clickable reference: age gate, header, deal feed, deal card, settings sheet), `guidelines/*.card.html` (specimens), voice/copy rules.

## 2026-06-11 — Observations on import (pending Erik's confirmation where noted)

- Import is consistent with the product's real ADRs: ADR-009 (discount + gas cost shown side by side, never net savings), ADR-021 (keyboard a11y, 44px targets, WCAG AA), ADR-022 (sort tiers: timed happy hours by countdown, untimed happy hours, dailies by discount), ADR-024 (single gas formula home), ADR-026 (stale sources = one muted line).
- **Discrepancy:** import's `_imported-CLAUDE.md.txt` names `--radius-md: 8px` as the house corner; `tokens/spacing.css` defines `--radius-lg: 8px` as the default. Token files are internally consistent → **token files win** (provisional ruling; flag at Finalize).
- Brand stance captured in tokens: single action color green-700; amber strictly for urgency/expiring; flat hairline cards (no shadows on cards); mono tabular figures for all money/distance/time; 4px grid; sentence case; no emoji; not stoner-branded.

## 2026-06-11 — Discovery setup (Erik's picks)

- **Sources (inherited by reference):** `briefs/brief-Happy-2026-06-08/brief.md` + `addendum.md`, `prds/prd-Happy-2026-06-08/prd.md`, `architecture.md`, `epics.md`, and root `ADR.md` (28 ADRs). All of them — Erik confirmed.
- **Working mode:** Fast path — batch gaps, draft both spines with `[ASSUMPTION]` tags, skip creative tools.
- **Stakes:** Consumer — real public users post-R&D. Full accessibility floor, calm error states, age-gate rigor.

## 2026-06-11 — Fast-path gap answers (Erik)

- **Form factor:** phone-first; on larger screens the single column simply centers on the gray page. The surrounding desktop space is deliberately unspecced for now — Erik may use it later (possibly ads). Spec it as a future consideration, not a layout.
- **Dark mode:** deferred — light-only on purpose; dark mode is a stated later addition (future consideration in DESIGN.md).
- **Journey protagonist:** "Stacy" — named protagonist for the key flow.
- **Provenance note (verified live):** the import was produced by Erik's claude.ai/design project "Gma's Helper Design System" (projectId `45dd119c-3292-484c-9755-cb2d901c3ca5` — matches the bundle global `GmaSHelperDesignSystem_45dd11`). DesignSync plugin confirmed connected and writable 2026-06-11; remote project last updated Jun 11 evening. Future design changes can round-trip via /design-sync.

## 2026-06-12 — Finalize: distillation + reconciliation

- Spines drafted (status: draft) by distillation subagent; Pass 1 coverage checks green. 1 assumption tagged (countdown excluded from aria-live), 5 open questions.
- `reconcile-gmas-helper-design-system.md` written: 8 overrides (spines win), dropped-ideas list for Erik's review, import-errors list (mockMpg, fictional prop tables in import prose, hardcoded staleCount — `.d.ts` + `components.css` are the real contracts).
- **OQ-2 resolved:** "Starts at HH:MM" future happy hours — Erik already deferred this in Story 3.2 (needs server filter change; lives in deferred-work.md). Future consideration, not a spec blocker.
- **OQ-4 resolved:** vehicle-cascade loading indicators/timeout messaging — ADR-028 deferred items. Future consideration.
- **OQ-5 half-resolved:** shipped `VehicleSelector.tsx` auto-applies on Model selection and collapses — no Save button; spine's FR-8 reading confirmed. REMAINING: sheet-vs-inline presentation (below).
- **Implementation delta noted:** shipped gear button is a literal ⚙️ emoji — violates the design system (no emoji; Lucide `settings` icon). To fix when the system is applied to the app.

## 2026-06-12 — Finalize triage rulings (Erik)

1. **Vehicle settings presentation: bottom sheet with dimmed backdrop** — ruled. The shipped inline panel (Story 3.2) is now a known implementation delta: the app must be reworked to the sheet when the design system is applied (alongside the ⚙️-emoji → Lucide gear fix).
2. **Manual refresh button: none.** ADR-022 single-fetch stands; PRD §4.1 prose loses. Logged as future consideration.
3. **Empty state: keep the generic** "No active deals right now" — no filter-aware variant. (ADR-025 Ask-First satisfied: asked, declined.)
4. **Reviewer Gate: opted in, full lens set** — rubric walker + accessibility (consumer stakes) + regulated-language/content (21+ cannabis context).

## 2026-06-12 — Reviewer Gate run + fix batch (Erik approved)

- Three lenses ran (rubric, accessibility, regulated content) → `review-*.md`; consolidated `validation-report.{html,md}`. Verdict: structurally strong; 0 critical / 7 high / 12 medium / 14 low.
- **Fix batch applied to both spines (all 25 report fixes):** new `border-field` token (#6b7280) for TextField/Select boundaries; slider 44px hit target + tap-to-set + aria-valuetext + debounced "N deals within X miles" announcement; Document semantics block (lang, title, landmarks, h1/h2); Notice `default` fg pinned to text-body; amber-badge pair pinned (4.510:1); stale badge → gray-600; shadows restated inline; sticky-header scroll-padding; sheet focus contract completed; cascade "Loading models…" status; Selects stacked one per row (both spines); skeleton aria-hidden/aria-busy; offline state row; focus-on-expiry rule; footnote status-region ordering; `<time datetime>`; em-dash DOM note; Honest Math rule 6 (scraped-copy display constraint); persistent disclaimer footer copy; Linda beat sentence; ads platform-risk note; dates bumped.
- **Verify-with-counsel register (logged, not fixed):**
  1. Does republishing retailer promotional copy make the aggregator an "advertiser" under WAC 314-55-155? (Rule 6 constrains display meanwhile.)
  2. Do any WA mandatory-warning statements apply to a non-commerce aggregator? (Disclaimer footer reserves a slot.)
  3. Age-gate decline path: ADR-021's no-decline ruling stands; defensibility to be confirmed before public launch (norm is a two-option gate).

## 2026-06-12 — Final rulings (Erik)

- **Countdown aria-live: ruled.** Card countdowns tick silently for screen readers; only the footnote status lines announce. Assumption tag removed from EXPERIENCE.md.
- **Mock coverage: import visuals suffice for now.** Every IA surface is covered by `imports/gmas-helper-design-system/ui_kits/app/index.html` + guideline cards; no fresh mockups rendered. Spine-only deltas vs the import demo (stacked Selects, disclaimer footer, no Save button) are governed by spines-win-on-conflict. `.working/` stayed empty (fast path); nothing to promote.

## Open items

- [x] Radius-token ruling — confirmed: token files win (radius-lg: 8px house corner).
- [x] Discovery — completed via fast path 2026-06-11/12 (form factor, dark mode, protagonist, sources, stakes all ruled above).
