# Accessibility Review — Happy (consumer stakes, WCAG 2.2 AA)

Reviewer: adversarial accessibility lens. Contract under review: `DESIGN.md` + `EXPERIENCE.md` (status: draft). Ratios below are computed from the hex values in DESIGN.md frontmatter (WCAG relative-luminance arithmetic), not taken from the spec's own claims.

## Verdict

The spine is well above industry baseline — the declared text pairs genuinely pass AA, dialog semantics are specified, and the calm-by-default live-region policy is defensible. But the spec self-contradicts on its strongest claim ("targets ≥ 44px for every interactive element" vs. a 22px slider thumb with no stated hit-target), leaves form-field boundaries at 1.47:1 against the 3:1 non-text floor on the primary cascade flow, and is silent on document-level basics (title, lang, landmarks, headings) plus several WCAG 2.2-specific criteria (2.5.7 dragging alternative, 2.4.11 focus-not-obscured under a sticky header). Nothing here forces a redesign; everything is fixable with token and wording changes.

## Contrast audit table

Computed values. AA floor: 4.5:1 text (< 18.66px regular / < 24px any weight here — nothing on these surfaces qualifies as large text), 3:1 non-text (1.4.11).

| Pair | Ratio | AA? | Used for | Note |
|---|---|---|---|---|
| text-body `#374151` on white | 10.31 | PASS | body copy on cards | |
| text-body on gray-50 `#f9fafb` | 9.86 | PASS | body on page surface | |
| text-strong `#111827` on white / gray-50 | 17.74 / 16.98 | PASS | headings, dispensary names | |
| text-muted `#6b7280` on white | 4.83 | PASS | 14px metadata on cards | spec claim verified |
| text-muted on gray-50 | 4.63 | PASS | slider ticks, footnotes on page | thin margin — do not darken gray-50 |
| text-muted on surface-sunken `#f3f4f6` | **4.39** | **FAIL** | Notice `default` box (sheet MPG line) **if** fg is muted | DESIGN.md Notice spec omits fg — must pin to text-body |
| text-muted on amber-50 `#fffbeb` | 4.66 | PASS | window text on urgent card | |
| white on green-700 `#15803d` | 5.02 | PASS | primary button, discount chip | spec claim verified |
| white on green-800 / green-900 | 7.13 / 9.11 | PASS | hover / active | |
| green-700 on white / gray-50 / amber-50 | 5.02 / 4.80 / 4.84 | PASS | link, discount %, sheet car icon | |
| text-urgent amber-700 `#b45309` on amber-50 | 4.84 | PASS | countdown on urgent card | |
| amber-700 on amber-100 `#fef3c7` | **4.51** | PASS (by 0.01) | urgent badge, 12px uppercase | knife-edge; any token drift fails it |
| text-error `#b91c1c` on red-50 / white | 5.91 / 6.47 | PASS | error Notice | |
| white on gray-900 `#111827` | 17.74 | PASS | age-gate attestation | |
| green-300 `#86efac` on gray-900 | 12.63 | PASS (non-text) | age-gate shield icon | |
| gray-600 on gray-100 (neutral badge) | 6.87 | PASS | "Daily deal" 12px | |
| gray-500 `#6b7280` on gray-100 `#f3f4f6` | **4.39** | **FAIL** | `stale` badge text, 12px | specimen-only in v1 (ADR-026 omits stale badging) but it is a specced variant |
| green-800 on green-50 (fresh badge) | 6.81 | PASS | | |
| status-fresh green-600 dot on white / gray-50 | 3.30 / 3.15 | PASS (non-text) | fresh dot | barely on gray-50 |
| status-stale gray-400 `#9ca3af` on white | **2.54** | **FAIL** (non-text) | stale status dot, wherever used | badge `stale` dot uses currentColor gray-500 (4.83 OK); the raw token fails |
| border-strong `#d1d5db` on white | **1.47** | **FAIL** (non-text) | TextField/Select boundary | the only visual indicator of field extent — 1.4.11 |
| border-default `#e5e7eb` on white / gray-50 | 1.24 / 1.18 | n/a | card hairlines | cards are non-interactive in v1 → 1.4.11 does not apply; informational |
| amber-200 border on amber-50 | 1.20 | n/a | urgent card tint border | urgency also conveyed by badge text — not color-alone, OK |
| slider track gray-200 on gray-50 | 1.18 | marginal | range extent | thumb (green-700, 4.80) is the required indicator; track exempt-ish, see finding |
| focus-ring green-700 on white / gray-50 / amber-50 | 5.02 / 4.80 / 4.84 | PASS (non-text ≥3:1) | 2px ring, 2px offset | white-gap ring on filled controls also passes |
| gray-500 chevron on white | 4.83 | PASS (non-text) | Select chevron | |

## Findings

- **[high]** Form-field boundary contrast fails 1.4.11: `border-strong #d1d5db` on white is **1.47:1** against the 3:1 non-text floor, and the 1px border is the only indicator of TextField/Select extent (DESIGN.md → Components → TextField/Select; `action-secondary-border` shares the token, but secondary buttons have a text label so the border is not load-bearing there). The Year→Make→Model cascade is a primary flow (Flow 2). *Fix:* introduce `border-field: '#6b7280'` (gray-500, 4.83:1) for TextField/Select borders — or at minimum require a second ≥3:1 affordance (filled `surface-sunken` field background plus boundary) and state it in the Select/TextField specs. Leave `border-strong` for the secondary-button outline where it is decorative.
- **[high]** Slider thumb target contradicts the spec's own floor and risks 2.5.8: EXPERIENCE.md → Accessibility Floor declares "Targets ≥ 44px for every interactive element," but DESIGN.md `range-slider.thumb-size: 22px` is below even the 24px AA minimum (2.5.8 Target Size), and nowhere does either spine say the *hit target* is larger than the painted thumb. A lone slider may scrape by on the spacing exception, but the spec's stated 44px contract is unmet as written. *Fix:* add to `range-slider`: `thumb-hit-target: 44px (transparent expanded hit area; painted thumb stays 22px)` and a matching sentence in EXPERIENCE.md → Component Patterns → RangeSlider.
- **[high]** No single-pointer non-dragging alternative specced for the slider (2.5.7 Dragging Movements): EXPERIENCE.md offers "drag/arrow the slider" — keyboard does **not** satisfy 2.5.7, which requires a single-pointer alternative to dragging. *Fix:* spec the control as (or behaving as) a native `input[type=range]` where a tap on the track sets the value directly, and state "tap-to-set on track" explicitly in the RangeSlider behavioral rules. (Native range also gets this for free.)
- **[high]** Notice `default` foreground is unspecified and the obvious token fails: DESIGN.md → Components → Notice gives `default` a `surface-sunken` box with no fg color; if implementers reach for `text-muted` (the Notice family's signature color per the `muted` variant), the result is **4.39:1 — below AA**. This is the sheet's resolved-MPG line ("Estimated 32 MPG…"), a key confirmation. *Fix:* pin `notice.default` fg to `{colors.text-body}` (9.4:1 on sunken) in the component spec; add "text-muted never sits on surface-sunken or gray-100" to Do's and Don'ts.
- **[high]** Document-level requirements are absent from both spines: no page title (2.4.2), no `lang` (3.1.1), no landmark structure or heading hierarchy (1.3.1, 2.4.1/2.4.6). Is the wordmark an `h1`? Are dispensary names `h2`/`h3`? Is the feed a `main` landmark with the header a `banner`? The spec is silent. *Fix:* add a short "Document semantics" block to EXPERIENCE.md → Accessibility Floor: `<html lang="en">`; `<title>Gma's Helper — cannabis deals worth the drive</title>` (or similar, sentence case); landmarks `banner` / `main`; visually-hidden `h1` ("Gma's Helper") with dispensary names as `h2` headings so SR users can jump card-to-card.
- **[medium]** Sticky header can obscure focused elements (2.4.11 Focus Not Obscured — Minimum, new in 2.2): EXPERIENCE.md specs a sticky header but says nothing about keyboard-scroll behavior; tabbing backward up the feed will scroll focused cards/controls under the header. *Fix:* spec `scroll-padding-top: <header height + 8px>` on the scroll container in the Header behavioral rules.
- **[medium]** Slider accessible name/value semantics unspecified (4.1.2 / 1.3.1): label "Within", value "25 miles" — a native range announces only "25". *Fix:* add to RangeSlider rules: accessible name "Within" (programmatically associated label) and `aria-valuetext` of "25 miles" / "1 mile" matching the visible singular/plural copy.
- **[medium]** Feed re-filter and re-sort are silent to screen readers: dragging the slider from 25→10 silently drops cards; only the *empty* outcome announces (the empty-state line is `role="status"`). A SR user gets no confirmation the filter did anything until it does everything. *Fix:* add one polite, debounced status line to the feed (visually-hidden or doubling as visible copy): "N deals within X miles" — re-announced on settle, not per-tick of the drag.
- **[medium]** Vehicle-applied confirmation is not in a live region: Flow 2's climax — MPG resolves, every card recalculates, the panel collapses — has no specced announcement; only failures (`role="alert"`) speak. *Fix:* make the sheet's resolved-MPG Notice (`"Estimated 32 MPG — gas cost will use your vehicle."`) `role="status"` so the confirmation is heard at the moment sighted users see it.
- **[medium]** Skeleton→content transition unannounced and skeletons unsilenced (4.1.3): nothing specs `aria-hidden` on SkeletonFeed or a loading/loaded announcement; a SR user on cold load hears nothing, then a feed exists. *Fix:* spec `SkeletonFeed` as `aria-hidden="true"` with the feed region carrying `aria-busy="true"` while loading; the always-visible "Last updated …" `role="status"` line then serves as the natural "loaded" announcement on resolve.
- **[medium]** Sheet focus contract is incomplete: "focus enters on open" names no initial target, and a focus *trap* is never stated for the sheet (it is stated for the gate). `aria-modal` hints at it but the contract should not rely on inference. *Fix:* EXPERIENCE.md → Settings sheet: "focus moves to the sheet title (or first Select) on open; focus is trapped within the sheet while open; all three close paths return focus to the gear" (the return is already specced — keep it).
- **[medium]** Focus loss when a focused card expires: countdown ticks can remove the card a keyboard user is currently on (expiry drops deals off mid-session), sending focus to `<body>` — disorienting and unspecced. *Fix:* add to the feed assembly rules: if the focused element is inside a removed card, move focus to the next card (or the slider when none remain).
- **[medium]** Cascade loading silence (consequence of a binding ruling — ADR-028 deferral, .decision-log.md 2026-06-12 OQ-4): "the selects sit disabled while a menu loads" gives SR and keyboard users zero feedback — a disabled Model select after choosing Make is indistinguishable from a broken one. Not re-litigating the deferral of spinners/timeouts; flagging that *total silence* is the accessibility cost being accepted. *Fix (minimal, within the ruling):* one polite `role="status"` line in the sheet ("Loading models…") — copy only, no new UI.
- **[medium]** Three Selects "in a row" likely break at 320px reflow (1.4.10): DESIGN.md → Settings sheet specs three selects in one row; at 320px viewport that is ~90px per select — model names ("Grand Cherokee") will truncate or force horizontal scroll. *Fix:* spec the cascade as stacked (one select per row) below a stated breakpoint, or stacked always — three taps either way.
- **[low]** `stale` badge variant fails AA as specced (gray-500 on gray-100, 4.39:1 at 12px): unused in the v1 feed (ADR-026 omits stale dispensaries) but it ships in the system. *Fix:* change `stale` badge fg to `{colors.gray-600}` (6.87:1), matching the neutral badge.
- **[low]** `status-stale` token (gray-400, 2.54:1 on white) fails non-text contrast wherever it would render as a status dot. ADR-026's "deliberately non-intrusive" is the ruling; the consequence is that the token cannot legally carry meaning alone. *Fix:* note in DESIGN.md → Colors that `status-stale` must always be paired with text (it is today — "N sources unavailable") and never used as a sole indicator.
- **[low]** Urgent badge is one rounding error from failure: amber-700 on amber-100 = 4.510:1. *Fix:* none required; add a frontmatter comment pinning the pair ("do not lighten amber-700 / darken amber-100 — AA margin is 0.01") so token drift can't silently break it.
- **[low]** Age gate has no decline path (binding, ADR-021 — consequence flag only): focus is trapped in an alertdialog whose only exit is affirmation; an under-21 or non-consenting SR user has no in-page exit and no explanation of how to leave. 2.1.2 is satisfied only because the browser itself is the exit. *Fix (within the ruling):* one sentence of plain text inside the gate — "If you are under 21, please close this page." — no second button, no navigation.
- **[low]** Em-dash in "30% off — $1.46 to get there": most SRs skip or mis-voice "—", yielding "30% off $1.46 to get there" — momentarily parseable as a price for the discount. *Fix:* keep the visual em-dash; spec the DOM so the line reads as one sentence and note that an explicit `aria-label` of "30% off, $1.46 to get there" on the line is acceptable if testing shows confusion.
- **[low]** "Last updated Jun 10, 7:45 AM" (pinned en-US, ADR-022 — binding) omits the year and assumes the AM/PM convention; fine for the consumer audience, but spec the timestamp as a `<time datetime="…">` element so AT and reader modes get the unambiguous machine value.
- **[low]** Three sibling `role="status"` regions (last-updated, stale count, empty-state line) will all announce on initial load, queueing three polite messages. Acceptable, but *Fix:* note an ordering expectation (empty/“last updated” first, stale count second) or merge the footnotes into one status region.
- **[low]** Text spacing (1.4.12) risk on overline badges: 12px uppercase pills with fixed `4px/8px` padding may clip when users apply letter/word-spacing overrides. *Fix:* one line in DESIGN.md → Badge: pills size from content (no fixed width/height; padding, not clipping).

## What the spec gets right

The headline claims survive adversarial arithmetic: every declared load-bearing text pair (body/muted/strong on white and gray-50, white on the full green action ramp, urgent on amber-50, error on red-50) genuinely clears 4.5:1, and the focus ring clears 3:1 on every light surface with a specced white-gap fallback on filled controls. The age-gate dialog contract (alertdialog, aria-modal, focus-to-confirm, trap, feed absent from the DOM) is exactly right. Countdown exclusion from aria-live [tagged ASSUMPTION] is the correct call — a 60s polite announcement per card would be hostile. Scoping the fueleconomy failure to a panel-local `role="alert"` while the feed silently keeps the national average is textbook error containment. 44px controls, keyboard-complete interactions, reduced-motion collapsing to 0ms at the token level, real-text figures, type-by-badge-never-color-alone, and the 14px readable floor are all consumer-grade decisions stated as contract, not aspiration.
